spring:
  freemarker:
    check-template-location: false
  profiles:
    active: @activeProfiles@
  application:
    name: bmsalong-api-${spring.profiles.active}
  cloud:
    nacos:
      config:
        server-addr: 5.tcp.vip.cpolar.cn:12752 #nacos中心地址
        file-extension: yaml # 配置文件格式
        refresh-enabled: true
        enabled: true
        shared-configs:
          - data-id: bmsalong-common.yaml
      discovery:
        server-addr: 5.tcp.vip.cpolar.cn:12752
  data:
    redis:
      repositories:
        enabled: false
  #  pid:
  #    file: /自行指定位置/bsmalong.pid

  redis:
    #数据库索引
    database: ${REDIS_DB:1}
    host: ${REDIS_HOST:3.tcp.vip.cpolar.cn}
    port: ${REDIS_PORT:12659}
    password: ${REDIS_PWD:010701kk}
    #连接超时时间
    timeout: 5000